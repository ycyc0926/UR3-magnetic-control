# H 磁驱动方形任务进展补充

> 2026-09-20 策略更新：当前唯一全局净空为板底 5 mm、左右侧各
> 10 mm、桌面 10 mm，适用于正常轨迹、停车模型和在线检查。下文
> 3/18/20/23/100 mm 等数值是 2026-09-19 历史审核记录，不再是当前设置，
> 对应旧轨迹必须按新策略重新审核后才能使用。

更新至 2026-09-19 约 20:24，Asia/Shanghai。继续遵循
`HANDOFF_H_MAGNET_PATH_20260919.md` 的安全约束；本文件记录交接之后的授权、实施结果与当前停留点。

## 目标与当前结果

用户选定约 24 mm 方形、现场手动启停电机。方形未完成。用户现优先要求检验“电机旋转，同时磁铁沿 X 平移”能否驱动 H 直行。

- 用户批准的水平对齐 execution_02 已完成：实际世界位移约 [-16.392,+2.622,-0.034] mm；结束后 UR 停止，电机未运行，H 未有效移动。
- 该段规划 TCP 峰值约 1.037 mm/s，实测峰值约 4.889 mm/s。两者不能混同；根因尚未确定。新短移执行器增加了直接 RTDE TCP 速度模长 2 mm/s 中止，关节中止仍为 2 deg/s。
- 电脑于 19:17:24 重启，原进程退出。依此前服务恢复授权，19:24 恢复相机/H 跟踪及真实 UR 驱动，未由本任务重启电脑。
- 原会话 `camera/tracking_sessions/20260919_142716_351773` 保留；新会话为 `camera/tracking_sessions/20260919_192441_728726`。
- 19:25 首次录像准备出现一帧 207 ms 图像年龄，触发 200 ms 锁定；当时未通知开始，用户确认没有开电机或碰 H。阈值未放宽。
- 19:28 对齐后原转向单次试验：1414 帧连续跟踪，H 净变化约 0.011 mm，无有效推进，用户确认 H 没动。独立 RTDE 8986 帧速度全为零。
- 19:32 用户批准的反转单次试验：390 帧连续跟踪，H 净变化约 0.013 mm，无有效推进，用户确认仍不动。独立 RTDE 2558 帧速度全为零。
- 两次网页启停标记间隔分别 6.798、8.934 秒；不是电机实际时长。用户未给出这两次实际运行秒数，不能宣称准确完成了 6 秒。面板 10 rpm、无硬件转速/启停反馈。

## 当前几何与现场信息

球心模型约 [202.76,97.66,425.55] mm；板底 Z=490.741 mm，板上表面 Z=498.141 mm。球心到 H 平面约 72.65 mm，磁球顶部到板底约 55.20 mm。

实时关节角 + 本机校准 URDF 的最高碰撞点到板底：上臂约 50.07 mm、前臂 51.39 mm、腕 2 为 32.33 mm、腕 3 为 32.23 mm。暂定工具包络约 32.41 mm；各面外扩 5 mm 后约 27.26 mm。原始工具包络仍未完全实测，`execution_allowed`、`verified` 等配置未改真。

用户澄清此前约 20 mm 实际净空指机械臂腕部；模型值与现场读数分别记录。用户要求 link 距离由 URDF 直接计算，后续无需重复要求其测量已建模 link；自装工具、线缆和板面假设仍需区别对待。

只做过离线上抬空间筛查：+3 mm 时外扩模型板底余量约 24.27 mm，+6.5 mm 时约 20.77 mm。此为静态扫描，不是已授权的上抬轨迹。用户随后改提水平移动方案；未抬升。

## 已准备的单次 X 对照

目录 `robot/planning_checks/square24_xy_motor_probe_20260919_audit`，说明 `REVIEW.md`。

- 世界 −X 3 mm，Y/Z 与姿态不变；运动 5 秒、首尾各 0.25 秒停留，共 5.5 秒。
- 手动电机仍 10 rpm，沿用刚才反转档，最多约 6 秒；单次监督，不自动重试、串接或追踪 H。
- 规划峰值 TCP 1.125 mm/s，关节 0.2361 deg/s；实测 TCP >2 mm/s 或关节 >2 deg/s 中止。
- 693 个实际 JTC 插值样本 +972 个名义速度停车样本通过 FCL；另 640 个实测速度门限下的停车扰动样本通过。名义最小模型板底 27.26 mm，后者最小 22.25 mm；使用旧专项名义 23 / 停车 18 mm 的分层参考，绝非实测制动认证。
- 39 项相关离线测试通过，代码/审核文件哈希封存在 `EXECUTION_REVIEW.json`。默认运行仅只读预检。
- 8767 原视觉区仅观察/记录，其电机标记不触发机器人。8768 是单次执行器的控制后端。用户要求将按钮放到 8767 页面下方，20:01 已经另行获准重载视觉服务并应用；当前控制区显示禁用按钮，没有启动运动门控。

## 最近尝试和恢复点

`robot/planning_checks/square24_xy_motor_probe_20260919_execution_01`：19:45 只读预检、录像、单控制器激活与 External Control 启动均通过；在原位等待独立页面执行按钮 90 秒，未收到请求，超时取消。

**motion_sent=false；没有发送该 3 mm 轨迹。** 12430 个独立 RTDE 样本关节/TCP 速度全为零。程序已 STOPPED、NORMAL；运动控制器已 inactive；录像 1952 帧保存。该结果不能判断旋转加平移能否驱动 H。

用户表示没有看到按钮，随后说明没有打开新页面。已通过桌面 `xdg-open http://127.0.0.1:8768/` 打开一个只读按钮预览；预览不接受 POST、不连接 ROS 或机器人，最多保留 300 秒。

上述只读预览已经自然退出。用户仍未看到独立页面，要求把按钮放在原 8767 页面下方；没有因此批准失败后的运动重试。

## 8767 按钮修改与新的 H 起点

- `h_preview_web.py` 已在画面下方加入明确的真实机械臂控制区，页首有直达链接；未准备时显示禁用红色按钮，执行器就绪后嵌入单次控制页。断开/过期后恢复禁用，原视觉按钮不会提交运动。方形参考按钮改为用户选定的 24 mm。
- `explicit_probe_trigger.py` 新增只读状态与浏览器心跳；单次 token、同源检查、过期拒绝、重复拒绝和中止逻辑保留。执行器就绪提示链接改为 `http://127.0.0.1:8767/#arm-control`。
- 39 项机器人离线测试、11 项视觉测试通过；4 项独立 Firefox 实点测试通过，日志 `square24_xy_motor_probe_20260919_audit/embedded_ui_browser_tests_03.log`。前两次新测试因自动化没有把 iframe 滚入视窗而未点击到按钮，修正测试滚动后通过；未使用脚本代点绕过浏览器实点检查。测试只连接临时端口、无机械臂消费者，不操作用户浏览器/现场控制。
- 审查哈希已更新，旧封存文件为 `EXECUTION_REVIEW.before_embedded_ui.json`。新封存保持 `execution_allowed=false`，明确 `runtime_ui_applied=false`、`current_h_baseline_valid=false`。
- 19:52 日志记录失锁、两次重新选取；用户确认手动调整过 H 的朝向。19:59:44 只读球心约 [202.773,97.665,425.534] mm；H 相对球心约 [+1.153,+1.157] mm，水平距离 1.634 mm；相对旧 H 起点约 [+1.195,+1.174] mm。新观察保存在 `square24_h_reorientation_20260919_observation`，仅观察，未授权新运动。
- 旧短移的 H 起点限制为 0.5 mm，新观察被原检查明确拒绝。不得只改旧记录、放宽阈值或重选目标来让旧轨迹执行；需基于新现场重新准备。
- 19:57 直接 RTDE/Dashboard：STOPPED、NORMAL、所有关节和 TCP 速度零；随后只读 ROS 查询确认所有运动控制器 inactive。原视觉、UR 进程仍保留，没有发送运动、播放或停止命令。

应用 8767 新布局需要重新加载视觉程序。现有 `h_vision.launch.py` 把相机与跟踪进程的退出联动，因此申请了仅重载这一组视觉服务。用户明确答复“同意仅重载相机和 H 跟踪”；该许可不包含运动。

## 20:01–20:03 已批准的视觉重载

- 核对旧 PID/命令行、无正在录制 clip、UR STOPPED/NORMAL/速度零后，只向视觉 launch 4161 发送 SIGINT，旧相机 4190 和 H 跟踪 4192 正常退出。未向 UR 发送停止、播放或运动命令。
- 按相同 `h_vision.launch.py` 和相机配置重启视觉服务，未额外执行相机参数重置脚本。新 launch 18132、相机 18150、跟踪 18152；UR launch 4205、控制进程 4224 保留。
- 新会话 `camera/tracking_sessions/20260919_200151_443185`，旧会话与 1952 帧录像保留。先看新图像确认 H，再通过仅视觉 `/select` 恢复目标锁定；没有把新旧会话位置拼成运动轨迹。
- 新 8767 页面 GET 已核验下方真实机械臂区、禁用按钮及 24 mm 参考按钮。桌面已执行 `xdg-open http://127.0.0.1:8767/#arm-control`；未启动 8768 运动门控，按钮仍不可执行。
- 20:03 约 19.96 FPS、连续跟踪 837 帧、图像新鲜。H 约 [203.970,98.800] mm。独立 RTDE 与 Dashboard 复核 UR STOPPED/NORMAL/速度零，scaled controller inactive。
- 完整记录 `robot/planning_checks/vision_ui_reload_20260919_2001`，包括 before、shutdown、after_h_lock、post_reload_verification、summary 与 launch 日志。旧软件审核中的 `runtime_ui_applied=false` 是修改完成、尚未部署时的历史值；当前部署状态以本次报告为准。
- 原短移的旧 H 起点仍失效，重载不授予重试或新运动许可。下一步先以用户调整朝向后的 H 核验、离线准备具体短直线，再按交接要求取得具体运动许可。

后续路线：新 H 基线与几何核验 → 单次短直线验证磁铁平移能否带动 H → 验证直角换向（如需改变轴向则另做完整规划）→ 按约 24 mm 四边分段、以 H 的实测轨迹判断完成。当前没有已验证的全方形执行器或自动电机停止能力，不能直接串接四边。

## 20:04 后用户同意开始试验，新增对齐待确认

用户明确“可以开始试验了”，视为同意继续准备并实施讨论中的单次 −X 3 mm 旋转加平移试验，不重复索取这项许可。但实际新 H 投影距球心约 1.65 mm，原 probe 草案要求初始相距不超过 0.5 mm，故不能重放旧草案或放宽条件。

已离线准备电机关闭时的微对齐：世界 ΔX=+1.208653 mm、ΔY=+1.127900 mm、ΔZ=0，固定工具姿态；运动 15 秒、含停留共 18 秒。规划峰值 TCP 0.206647 mm/s，关节 0.059303 deg/s。目录 `square24_h_reorientation_alignment_20260919_baseline`、`square24_h_reorientation_alignment_20260919_audit`。

- 2252 实际 JTC 插值样本加 3132 名义停车样本共 5384 点 FCL 通过；最小板底模型间隙 27.2676 mm。
- 448 个基于每关节实测中止 2 deg/s 的极端停车敏感性样本通过；最小板底 22.3067 mm。专项名义 23 / 停车 18 mm，5 mm 工具放大；并非实机制动认证。
- `run_checked_xy_alignment.py` 增加与 probe 相同的直接 RTDE TCP 速度模长 2 mm/s 中止，保留所有原门限；补充验证关节速度小但 TCP 超速、无效速度也会被拒绝。42 项相关离线测试通过。
- `square24_h_reorientation_alignment_20260919_read_only/report.json` 于 20:06:28 只读预检通过，motion_sent=false，UR STOPPED/NORMAL，原控制器保持 inactive。尚未开始录像、播放或执行任何本次运动。
- 审查说明和哈希封存在新目录 `REVIEW.md`、`EXECUTION_REVIEW.json`，`execution_allowed=false` 保留。
- 已向用户仅就**新增的 +X/+Y 微对齐**请求具体许可，包含单控制器激活、播放 External Control、结束/异常停止核验；原因是方向与已同意的 −X 试验不同，交接 P5 要求具体动作许可。等待答复，不自动执行。

以下进展取代之前的待执行步骤，不重放旧对齐。

## 20:08 对齐中止、用户改阈值并转为直接 X 匀速试验

- 用户批准微对齐后，核对封存哈希，运行 `square24_h_reorientation_alignment_20260919_execution_01`。确实发送轨迹，约 2.87 秒后直接 RTDE TCP 速度 2.8497 mm/s 超过当时 2 mm/s 门限，中止；整段采样峰值 3.85054 mm/s，关节峰值 0.50757 deg/s。
- 取消目标和 Dashboard stop 均返回，运动控制器成功停用。即时停车核验遇到尚未为零的速度，原 report 如实保留错误。随后独立只读三次采样速度全部为零，STOPPED/NORMAL；只读 ROS 查询所有运动控制器 inactive。
- 实际世界位移约 [+0.02452,+0.04578,-0.01611] mm，没有完成 1.65 mm 对齐。755 RTDE 样本，114 帧录像且全部有对应跟踪；H 净变化约 0.0032 mm，无有效推进。详见该执行目录 `ANALYSIS.md`、`summary.json`、`post_stop_read_only_verification.json`。
- 用户明确要求“速度阈值修改为20mm/s”。已把共享 `MEASURED_TCP_SPEED_LIMIT_MM_S` 设置为 20，alignment/probe 执行器与新 probe 草案报告引用该常量。规划速度、每关节实测 2 deg/s、几何/跟踪/时效约束未放宽。42 项相关离线测试通过，包括 20 mm/s 边界、速度向量模长、多轴合成超速和无效数值。
- 因源码改变，旧封存哈希不再是当前版本，不能直接复用。历史 2 mm/s 执行/审核记录保持原样，不能改写为 20。
- 曾准备剩余微对齐 `square24_h_reorientation_alignment_20260919_retry20_audit`，位移约 [+1.164919,+1.112296,0] mm，15 秒，名义与停车 FCL 通过。但是对应只读预检因 Joint feedback invalid or stale 被拒绝，未发运动。
- 20:08:57 相机出现 331.1 ms 接收帧间隙，与 250 ms stream watchdog 失锁一致，诊断没有 image_timestamp_stale。保持停止，查看当前画面确认同一 H 后仅通过视觉 /select 重新锁定；保留失锁时失败快照。未改任何视觉时效阈值，未重启服务。
- 用户随后明确取消额外对齐/试转步骤，要求“现在让机械臂一直以匀速向 X 方向移动……不要再做多余的测试”，并表示由其开启电机后让机械臂匀速移动。当前主线已改为**从当前实际位置直接进行一段 X 匀速直线**；不要继续上述对齐或把旧 5 秒五次多项式轨迹称为匀速。
- 已说明 20 mm/s 为实测中止阈值，不是规划速度；提出 1 mm/s、10 mm、含起停约 11 秒，到点停臂、手动电机最多约 12 秒，需用户选择世界 +X 或 −X；另给出原 −X 3 mm/电机 6 秒选项。用户尚未选择方向/行程，问题仍待回复，不能凭电机启动推定方向。已经提示准备完成前保持电机关闭。
- 新匀速路径及执行器尚未完成，不能启动旧红色 −X 3 mm 门控或发旧轨迹。保留原硬件安全约束；仅进行必要的本段路径/停止检查，不再增加无关现场试验。

目前长期进程：视觉 launch 18132、相机 18150、H 跟踪 18152、UR launch 4205、UR 控制节点 4224。PID 仅为本次快照，恢复时须核验。

## 最新选择：直接 −X 100 mm，匀速 10 mm/s；停车门限待协调

用户随后明确选择“世界 −X，100 mm，匀速 10 mm/s”，取代之前未选定的 10 mm 候选；这是当前要准备的具体直线动作。仍不先做对齐/原地试转。之后又明确要求“关节中止阈值设置为 20 度/s”。

已按其要求把 `manual_motor_probe_guard.MEASURED_JOINT_SPEED_LIMIT_DEG_S` 与 TCP 常量分别设为 20 deg/s、20 mm/s，alignment/probe 执行器引用共享常量，新 probe 元数据也同步；规划速度并未改为 20。31 项相关回归加 1 项关节 ±20 deg/s 边界测试通过，未启动实际监测执行器。旧 2 deg/s 审核/执行记录不改写；旧封存哈希已失效。

本次文件位于 `robot/planning_checks/uniform_x100_10mms_20260919`，详见 `REVIEW_STATUS.md`。新的纯离线规划器 `robot/prepare_uniform_x100.py` 采用 1 秒起速 +9 秒匀速 +1 秒减速、首尾共 0.5 秒停留，总 11.5 秒；匀速 10 mm/s。3475 个名义及名义停车 FCL 样本通过，规划关节峰值 2.09721 deg/s，名义板底最小约 27.264 mm。

**尚未执行，不能直接播放：** 20 deg/s 门限下现有保守停车敏感性模型未通过，独立关节最坏组合出现穿过板底平面的样本。该模型未结合 TCP 20 mm/s 约束收紧，不能宣称实际必然碰板；当前也不足以确认停车满足交接约束。

已完成一个不改变请求行程/匀速速度的备用门限检查：关节 2.5 deg/s 时，停车最小模型板底 19.145 mm，1344 个停车样本 FCL/阳性对照/FK 一致通过；该值高于规划关节峰值且低于用户指定的 20。已询问用户是否接受 2.5，或保留 20 并暂不运动，尚待答复。**不能默默改用 2.5，也不能在 20 门限未通过停车审核时执行。**

当前仍缺新的 100 mm 匀速执行器/明确标注该动作的启动入口；不得启动旧 8768 的 3 mm 按钮。可考虑独立桌面对话框（本机 zenity 已安装）作为用户实体开启电机后的明确单次执行按钮，避免再次重载视觉服务；尚未实现或运行此方案。继续时先处理用户最新参数选择，再完成仅本段所需的执行检查与记录，不增加无关现场试验。

## 最新答复：关节中止门限设为 10 deg/s

用户答复“设置10度/s”，取代此前 20 deg/s 的配置及 2.5 deg/s 候选的待确认状态。已将共享软件关节中止门限改为 10，TCP 仍为 20 mm/s；世界 −X 100 mm、匀速 10 mm/s 的请求不变。离线规划器默认门限同步引用共享常量，新增审核也将包含该常量文件哈希。29 项相关离线测试全部通过，包括 ±10 deg/s 边界、10.01 deg/s 拒绝和 TCP 速度约束。

使用已保存路径和原几何模型进行唯一新增的停车复算：`uniform_x100_10mms_20260919/STOP_SENSITIVITY_10_REQUEST.json`，1344 样本，最小板底模型间隙 −20.570902 mm，未满足专项停车 18 mm。模型仍是独立关节最坏组合，尚未结合 TCP 20 mm/s 约束收紧；这是现有审查未通过，不是实机必然碰板的断言。板底检查失败后没有重复运行 FCL，没有通过降低几何余量放行。

当前软件门限保持用户指定的 10 deg/s，没有擅自改为 2.5；2.5 候选通过的历史模型结果保留。2.5 是异常中止门限，不会降低所请求的匀速速度，规划关节峰值约 2.10 deg/s。尚未执行本次 100 mm 运动，没有发送播放/轨迹/控制器切换、停止现有进程或重启 ROS；未启动运动门控，电机仍要求用户保持实体关闭。当前未具备通过停车审核的新 100 mm 执行方案。

## 21:01 固定 Z 对齐执行中止，随后门限改为 5 deg/s

用户同意 0.5 deg/s 后，固定 Z 对齐的 60 s 轨迹完成离线与只读预检：世界 ΔX=-24.506760、ΔY=-3.156811 mm，规划关节峰值 0.192421 deg/s、TCP 0.772164 mm/s；名义板底 20.268833 mm，0.5 deg/s 停车敏感性 18.531862 mm，FCL/阳性对照通过。此次许可确实用于一次执行。

轨迹发送后约 4 s，独立 RTDE 测得关节峰值 0.627319 deg/s，超过 0.5 deg/s，执行器中止。末端只发生约百分之一毫米量级变化，未完成对齐。Dashboard stop 返回，运动控制器停用；内部即时核验因速度尚未归零报错，随后独立三次 RTDE 均为零、STOPPED/NORMAL、所有运动控制器 inactive。录像 `clip_20260919_210119_455739`；详见执行目录 `ANALYSIS.md`。不自动重试。

用户随后明确“关节速度阈值修改为5度/s”。共享关节实测中止门限现为 5 deg/s，TCP 保持 20 mm/s，29 项相关监控测试通过。当前固定 Z 路径的 704 个 5 deg/s 停车敏感性样本最小板底 3.368866 mm，未达到 18 mm；没有样本穿过板底平面，但余量不足。原 0.5 deg/s 审核与当前门限不一致，已失效。未再次播放或发送轨迹。

## 21:13–21:20 当前位置的 −X 100 mm / 10 mm/s 新方案

用户同意取消固定 5 deg/s 形式，改为轨迹相关速度监控。当前方案不使用固定关节绝对速度门限；每关节实测速度必须位于该时刻控制器期望速度 ±0.25 deg/s，TCP 线速度仍以 20 mm/s 中止，路径误差、直接 RTDE、视觉走廊和在线全臂/工具几何检查保留。

用户手动放置 H 后视觉曾 `locked_out`，查看实时画面确认同一 H 后只通过 8767 `/select` 恢复锁定，未重启服务。新基线目录 `uniform_x100_current_20260919_2113`：球心 [243.488041,99.442858,432.818718] mm，H [242.471114,100.831023] mm；目标球心 [143.488041,99.442858,432.818718] mm。路径为 1 s 起速、9 s 匀速 10 mm/s、1 s 减速，首尾停留后 11.5 s；实际执行还会加入 2 s 当前关节位置到封存起点的低速锚定段以避免此前启动修正脉冲。

1495 个控制器插值样本及 1980 个名义停车样本、总 3475 FCL 样本通过；轨迹相关速度包络的 3776 个停车样本通过，最小板底 18.897621 mm。名义板底 20.272057 mm。规划关节峰值 3.272052 deg/s、TCP 10.0000001 mm/s。31 项相关测试通过，只读实机预检通过。

第一次进入 90 s 操作员等待时，没有开始录像、没有收到电机标记、`motion_sent=false`。约 37 s 后 Dashboard 后台轮询短暂超时，系统中止等待并 stop；独立核验速度零、STOPPED/NORMAL、运动控制器 inactive。等待逻辑已修正为原位阶段依靠直接 RTDE/几何/视觉持续检查，并在接受录像及电机标记、发送轨迹前重新强制检查 Dashboard。按失败不自动重试规则，等待用户批准重新进入等待。

## 20:40 后：用户改为磁球 XY 对齐到 H，明确保持当前 Z

用户要求“把磁铁xy的位置调整到H的下方”，随后明确“不需要改变z的高度”。此次目标是固定当前高度/工具姿态的 XY 对齐，不是执行此前 100 mm 直线。只读发现机械臂及 H 均与上次记录不同；用户承认手动调整，随后明确确认相机/平台未动、电机实体关闭、板下人员已撤离。不要继续询问这三项，除非后续出现新的现场变化证据。

新观测目录 `robot/planning_checks/h_xy_alignment_request_20260919_204043_baseline`。球心 [243.518489,99.421696,432.828155] mm，H [219.011729,96.264885] mm；需要世界 ΔX=-24.506760、ΔY=-3.156811、ΔZ=0 mm，总长 24.709244 mm。当前 Z 比先前对齐/100 mm 审核基线高约 7.3 mm，旧方案和旧现场几何检查均不能重放。

只读 ROS/RTDE/Dashboard：所有运动控制器 inactive、STOPPED/NORMAL、速度零。固定 Z 的 51 点 IK 诊断已完成，名义模型板底最小 20.268834 mm，低于现有 XY 执行器的 23 mm 审查条件。关节 10 deg/s、TCP 20 mm/s 不变；704 个停车敏感性样本最小板底 -21.112977 mm，未满足 18 mm 条件。该独立关节保守模型未结合 TCP 约束收紧，不能宣称实际必然碰板。

没有实际运动，没有改变 Z 或阈值，没有重启/终止现有服务。未生成可执行轨迹；未为了通过检查而调低 23 mm 限制。现场确认见 `site_confirmation.json`，当前审查结论见 `REVIEW_STATUS.md`。用户保持 Z 的约束必须保留，当前尚无通过运动及停车审核的可执行对齐方案。
